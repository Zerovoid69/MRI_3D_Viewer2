# MRI 3D Medical Viewer - Python dependencies
#
# Install with:
#     pip install -r requirements.txt
#
# Tested with Python 3.10 - 3.12 on Windows 10/11.
# Lower bounds reflect the features/APIs this project actually uses; upper
# bounds are left open (or capped at the next major version) rather than
# pinned tightly, so `pip install` can pick up current compatible releases.

# --- GUI ---------------------------------------------------------------
PySide6>=6.6

# --- Numerical / scientific computing -----------------------------------
numpy>=1.26
scipy>=1.11
scikit-image>=0.22

# --- Medical image IO -----------------------------------------------------
SimpleITK>=2.3
nibabel>=5.2
pydicom>=2.4

# --- 3D visualization / mesh processing ------------------------------------
pyvista>=0.43
pyvistaqt>=0.11
vtk>=9.2

# --- 2D plotting (embedded slice viewer) ------------------------------------
matplotlib>=3.8
